{
  "buildCommand": "npm run build -- --no-lint",
  "devCommand": "npm run dev",
  "installCommand": "npm ci --prefer-offline --no-audit --fund=false",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; fi; if [ -n \"$VERCEL_PULL_REQUEST_ID\" ]; then exit 1; fi; pr_count=$(curl -s -H 'Authorization: token $GITHUB_TOKEN' 'https://api.github.com/repos/adrian-herscu/flight-club/pulls?head=adrian-herscu:$VERCEL_GIT_COMMIT_REF&state=open' | jq 'length // 0'); if [ \"$pr_count\" -gt 0 ]; then exit 1; fi; exit 0"
}
