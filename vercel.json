{
  "buildCommand": "npm run build -- --no-lint",
  "devCommand": "npm run dev",
  "installCommand": "npm ci --prefer-offline --no-audit --fund=false",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; fi; if [ -n \"$VERCEL_PULL_REQUEST_ID\" ]; then exit 1; fi; exit 0"
}
